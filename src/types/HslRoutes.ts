export type HslRoutesDefault = {
    data: {
        routes: [
            {
                gtfsId: string;
                longName: string;
                mode: string;
                shortName: string;
            }
        ];
    };
};